server:
  port: 8768

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: admin
    url: jdbc:mysql://127.0.0.1:3306/auth?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&
  application:
    name: shrek-codegen

mybatis:
   mapperLocations: classpath:/mapper/*.xml

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/